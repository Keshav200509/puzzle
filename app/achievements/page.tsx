import { AchievementsClient } from '@/components/achievements-client';

export const metadata = { title: 'Achievements & Badges — The Grid' };

export default function AchievementsPage() {
  return <AchievementsClient />;
}
